//libraries
#include<iostream>
//imports
#include"Menu.h"

using namespace std;

int main() {

	menu();

	return 0;
}

// Run program: Ctrl + F5 or Debug > Start Without Debugging menu
// Debug program: F5 or Debug > Start Debugging menu

// Tips for Getting Started: 
//   1. Use the Solution Explorer window to add/manage filescv
//   2. Use the Team Explorer window to connect to source control
//   3. Use the Output window to see build output and other messages
//   4. Use the Error List window to view errors
//   5. Go to Project > Add New Item to create new code files, or Project > Add Existing Item to add existing code files to the project
//   6. In the future, to open this project again, go to File > Open > Project and select the .sln file


	// string
	//string s(10, '$'); seteaza 10 de $
	//string xxx;
	//string s(xxx,5,4); // extrage din xxx incepand de la al 5-lea caracter 4 elemente
	//string s(xxx.begin()+1, xxx.end()-1); //copiere la nivel de pointer (begin - adresa primului simnbol)
	//getline(cin, xxx); //forma poo la string
	//getline(cin, xxx, 'a'); //se opreste cand intalneste 'a'
	//xxx = "step it";
	//cout << xxx;
	//xxx += " adrian"; //concat

	//cout << xxx.at(1);

//int a = 123, * pa;
//pa = &a;
//*pa = 1234;
//cout << a;
//a=1234